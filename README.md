A simple PySide6 app that generates a Vim config. Inspired from MX Linux's Bash Config tool. 
## How to run
```
cd <project directory>
pyside6-uic form.ui -o ui_form.py  # this is optional if you don't want to touch the GUI and is really not needed for it to run
python3 mainwindow.py
```


